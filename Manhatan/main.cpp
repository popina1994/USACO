#include <iostream>
#include <cstdio>
#include <cstring>
#include <cctype>
#include <string>

using namespace std;

const int n_max = 200, l_max = 30, h_max = 30;

char s[n_max + 1], c;

int main() {
    scanf("%d%d", &l, &h);
    scanf("%s", &s);

    return 0;
}
